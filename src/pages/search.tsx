import React from 'react';
import Layout from "@/components/layout/Layout";

const Search = () => {
    return (
        <Layout>
            <h1>В разработке...</h1>
        </Layout>
    );
};

export default Search;